version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - deploy:
          command: aws cloudformation deploy --template-file .cloudformation/template.yml --region eu-west-1
